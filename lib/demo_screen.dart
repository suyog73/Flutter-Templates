import 'package:flutter/material.dart';

class DemoScreen extends StatelessWidget {
  static const route = "/Demo Screen";
  const DemoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
